const num = parseInt(prompt("Ingresar un Número: "));
const resultado = (num % 2 == 0) ? "Es divisible por 2." : "No es divisible por 2.";
alert(`${num} ${resultado}`)
/* Escribe un programa que pida un número y diga si es divisible por 2 */